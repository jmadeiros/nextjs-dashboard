"use client"

import { useState } from "react"

// This is a simplified version that doesn't show any warnings
export default function EnvChecker() {
  const [showAll, setShowAll] = useState(false)

  // Always return null - effectively disabling the component
  return null
}
